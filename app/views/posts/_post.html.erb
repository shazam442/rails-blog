<div id="<%= dom_id post %>">
    <h2><%= post.title %></h2>
    <h6>by: <%= link_to post.user.name, user_path(post.user) %></h6>
    <div>
        <%= post.body %>
        <h6><%= pluralize(post.views, "view")  %></h6>
    </div>
</div>
